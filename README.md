# OSAssignment
Create a program to solve the readers writers problems using mutex's and semaphores.
The program allows multiple readers to read the data but only one writer to manipulate the data at once.
completed using processes and threads.

